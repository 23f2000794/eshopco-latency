[project]
name = "eshopco-latency"
version = "0.1.0"
description = "FastAPI telemetry endpoint for eShopCo"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "numpy"
]

[tool.vercel]
entrypoint = "api.latency:app"
